# Dependências de produção (servidor WSGI). Em cima das de runtime:
#   pip install -r requirements.txt -r requirements-prod.txt
-r requirements.txt

# Linux / Docker:
gunicorn==23.0.0
# Windows (alternativa pura-Python ao gunicorn):
waitress==3.0.0
# Driver PostgreSQL (produção; o binário já traz a libpq):
psycopg2-binary==2.9.9
# Observabilidade (opcional — ativa com SENTRY_DSN):
sentry-sdk[flask]==2.20.0
